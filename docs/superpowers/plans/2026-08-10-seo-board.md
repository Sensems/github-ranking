# Board SEO Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship Option B SEO (meta + OG/Twitter + canonical + robots + sitemap + JSON-LD) and wire `public/logo.png` as favicon, header logo, and `og:image`.

**Architecture:** Shared `useBoardSeo` composable sets head tags from `BOARD_META` + `runtimeConfig.public.siteUrl`; pages call it after fetch; Nitro routes serve `robots.txt` / polished `sitemap.xml`.

**Tech Stack:** Nuxt 3 `useSeoMeta` / `useHead`, Nitro server routes, Vitest.

## Global Constraints

- Product name「GitHub Star 趋势榜」
- No `@nuxtjs/seo` module
- Canonical ignores filter query params
- Logo asset at `frontend/public/logo.png`

---

### Task 1: Assets + site chrome

- [x] Ensure `frontend/public/logo.png` exists
- [x] Favicon in `nuxt.config` / `app.vue` head
- [x] Replace header SVG with logo `<img>`

### Task 2: `useBoardSeo` + tests

- [x] Implement composable + pure helpers for canonical / JSON-LD
- [x] Unit tests for helpers

### Task 3: Discovery routes + env

- [x] `robots.txt.ts`
- [x] Polish `sitemap.xml.ts`
- [x] `.env.example` SITE_URL notes

### Task 4: Wire board pages

- [x] Replace per-page `useHead` with `useBoardSeo` on all five boards
- [x] Run unit tests
